# Demo Client for ASR API

Small demo client in a uv package that gets transcriptions from asr-api.

Called with the `../run_client.sh` script.

```
.
├── README.md           # This file
├── cv-decode.py        # Transcription script
├── pyproject.toml      # Dependencies
└── uv.lock             
```
